import { Sprout } from 'lucide-react'
import PagePlaceholder from '../components/PagePlaceholder'

export default function ExperimentsPage() {
  return (
    <PagePlaceholder
      title="Experiments"
      icon={Sprout}
      note="Dashboard of experiment cards arrives in Task 2."
    />
  )
}
